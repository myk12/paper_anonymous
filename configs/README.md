# Configurations

This directory stores system-level configuration inputs for Utopia deployments
and experiments.

Typical contents include:

- topology and fabric descriptions
- host and endpoint inventories
- experiment-specific deployment parameters

These files are consumed by the toolchain and orchestration scripts rather than
by the runtime dataplane directly.
