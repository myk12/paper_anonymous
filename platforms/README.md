# Platforms

This directory contains substrate platforms that Utopia builds on.

These components are implementation bases rather than the architectural center
of the repository. Utopia-specific runtime, toolchain, and fabric logic should
live in their corresponding top-level directories.

## Current Contents

- `corundum/`: imported Corundum codebase used as the base FPGA/NIC platform
