# Runtime

This directory contains synchronous dataplane runtime components.

Current runtime components include:

- `dni/`: endpoint-side deterministic network interface execution
- `fabric/`: scheduled network fabric runtime, including switch-side Tofino
  artifacts
